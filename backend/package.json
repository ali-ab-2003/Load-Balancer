{
    "name": "lb-backend-server",
    "version": "1.0.0",
    "description": "Backend server for load balancer project",
    "main": "server.js",
    "scripts": {
        "start": "node server.js $SERVER_ID $PORT"
    },
    "engines": {
        "node": ">=20.0.0"
    }
}
